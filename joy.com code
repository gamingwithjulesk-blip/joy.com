from ast import List
from numbers import Number
import random
from tkinter import messagebox, simpledialog
from turtle import color
color  = ("butter yellow, baby blue, baby pink", "mint green, lavender, silver", "orange, purple, black", "pink, blue, white", "pastel orange, pastel pink, pastel cyan", "dark blue, dark green, dark purple", "red, yellow, blue", "light pink, light blue, light green", "neon green, neon pink, neon yellow", "burgundy, navy blue, forest green", "peach, sky blue, lilac", "maroon, olive green, mustard yellow", "coral, turquoise, gold", "plum, teal, bronze", "salmon pink, mint green, lavender", "charcoal gray, blush pink, baby blue", "cobalt blue, emerald green, ruby red", "soft yellow, powder blue, pale pink", "steel gray, dusty rose, sage green", "bright orange, electric blue, hot pink", "cream, light gray, pastel purple", "maroon, orange, yellow", "light blue, pink, yellow", "orange, purple, green", "orange and green", "cyan and purple", "red and black", "pink and lavender", "green and lavander", "orange and indigo", "yellow and teal", "brown and green", "neon green and neon pink", "light orange and light blue", "brown and rainbow", "green and black" "orange, teal, and purple" , "pink, yellow, purple" , "green, purple,red" , "black and rainbow" , "black and pink" "black and yellow" , "green, white, pink" "pink, blue, green, yellow" , "rainbow", "yellow and green" "red, brown, blue" "pink, black, green" , "purple, red, yellow" , "green, red, yellow" , "orange, white, green" "red, black, yellow, white" , "yellow, pink, blue, purple" , "red, yellow, green, orange" , "black, yellow, red, blue" "brown and light blue" , "teal, maroon, white" "orange,blue,yellow", "green, brown, blue, pink" , "neon colors", "pastel colors", "muted colors", "green, ornage, blue", "yellow, red, blue" , "forest green, red, white" , "coral, teal, white", "navy blue and gold" , "light blue and peach", "red, green, brown" , "purple and gold", "green, red, blue, brown" , "yellow, black, green" ,"light yellow, dark blue, pink" , "orange, blue, purple" , "red, orange, yellow, blue" , "mint green, purple, light pink", "gray, purple, brown" , "green, yellow, red, orange" , "purple, green, orange" , "orange, green, blue", "blue, pink, yellow", "white, yellow, blue", "maroon, light blue, white", "red, yellow, pink, white" , "orange,red,blue,green" , "orange, green, yellow" , "gold, light pink, white" , "brown, pink, blue" , "clean something" , "green, orange, purple" , "light blue, light green, white" , "teal, yellow, pink" , "gray, blue, white" , "dark blue, dark pink, light blue" , ")      
def generate_color_palette():
    palette = random.choice(color_palette) # type: ignore
    colors = palette.split(", ")
    return colors 

from ast import List
from numbers import Number 
import random
from tkinter import messagebox, simpledialog
def generate_screen-free_activity():
screen-free activity = ("go on a nature walk", "draw something", "read a book" , "go on a color scavenger hunt", "make a presentation about something you love", "bake something" , "try a new hobby" , "go to a local park" , "have a picnic" , "make a cardbourd craft" , "go geocaching" , "climb a tree" , "research a subject you know nothing about" , "decorate a picture frame", "find a pen pal" , "write a letter" , "paint something", "make something out of trash" , "listen to the radio", "write a song just for fun", "go to the libary and get a book on a topic thats new to you" , "make a calender" , "make a painting out of plants" , "press flowers" , "make a flower crown from real flowers" , "write a bucket list" , "make/learn a dance", "make a junk journal page out of recycling", "make a collage", "make your own watercolor paints" , "pick up litter in your negibour hood", "make braclets", "decorate your belongings", "make a sock puppet and a little play for them", "do a puzzle", "make duck tape art", "draw your dream outfit on paper", "make a scrunchie", "make custom trading cards", "design a board game", "draw your curent emotions" , "make a greek theater mask" , "draw you childhood stuffie" , "make a patch", "decorate clothing" , "decorate your phone case" ,"learn a new hairstyle", "make a headband" , "make a belt chain" , "make a keychain", "write afrimations for yourself", "make a vison board", "make up a card game" , "make jewlery" , "journal" , "decorate candles" , "make something from clay" , "do chalk art" , "play skiprope", "make a pillow/blanket fort" , "make slime" , "make a mini terarium", "draw yourself", "make yourself a sensory bin", "make a cardboard sculpture" ,"build a stick fort" , "make a garland" , "sew a plush" , "make a keychain" ,"play hopscotch", "blow bubbles" , "make a set list of your favourite songs on paper" , "start a journal about all of you favourite things" , "write a song about your favourite book" , "write an au of your favourite show/movie/book" , "listen to a genre of music you never have before" , "make art out of nature" , "go on a walk and try to find a cool rock")


from ast import List
from numbers import Number 
import random
from tkinter import messagebox, simpledialog
def generate_song_recommendation():
    songs = ("roses by iris copperman", "the hand by anabelle dinda", "dog prophecies by josie edwards", "orbiter by noah kahan", "the seed by aurora", "and the hound by yaelokre", "cryptid by cavetown", "preybirds by rabbitology", "DNA guarantee by kodi rhianne", "november by sparkbird", "campfire god song by willownbrook", "little boy in the grass by aurora", "anticipate heartbrake by aurora", "elizabeth by cult summer", "never the muse ny elio mei", "my alcoholic freinds by the dresden dolls", "be nice to me by the front bottoms", "mawce by everybodys worried about owen", "blitzkrieg bop by the ramones", "bog bodies by rabbitology", "placing the blame by self", "suffering by amelie farren", "kitchen light by josie edwards", "drift away omnicord by trillan", "the way you make me feel by michael jackson", "i want to hold your hand by the beatles", "doors by noah kahan", "poarch light by noah kahan", "the bird song by noah florce", "atlas by lilith max", "the cure by olivia rodrigo", "the lonliest girl in the world by noah florce", "hope ur ok by olivia rodrigo", "settle the score by cult summer" , "sweet tooth by cavetown" , "alien blues by vundabar" , "athenms for a seveten year old girl by yeule" , "august rain by willowbrook" , "smells like teen spirit by malia j " , "come as you are by niravana" , "star v by sofia isella" , "kitchen light by josie edwards" , "new house by josie edwards" , "high hopes by josie edward" , "lullaby by cavetown" , "dark by maddie ashman" , "house of wolves by my chemical romance", "she by green day" "american idiot by green day" ,"23 by noah kahan" ,"the great divide by noah kahan","at the cemetery by elio may and cloudkissed" , "watchers crown by cloudkissed", "devil town by cavetown", "juliet by cavetown" , "home by cavetown" , "500 miles by perter, paul, and mary" ,"the poet and her vice by josie eedwards" , "the moon will sing by the crane wives" , "good girls by josie edwards", "anarchy by egg", "turtle island by renee christine" , "cricket in the corner by lu la belle" , "stranger in you by ebril" , "sugar maple by ebril" ,"great escape by josie edwards", "running with scisors by cavetown", "white blank page by mumford and sons" , "animal by sir chloe" , "lavender by penelope scott", "girls by girl in red")   

from ast import List 
from numbers import Number
import random
from tkinter import messagebox, simpledialog
def generate_daily_affirmation():
    affirmations = ("you are worthy of love and respect", "you are capable of achieving your goals", "you are strong and resilient", "you are deserving of happiness and success", "you are enough just as you are", "you have the power to create positive change in your life", "you are surrounded by love and support", "you are grateful for the abundance in your life", "you are confident in your abilities",("you are at peace with yourself and the world around you"),("you are a unique and valuable person"),("you are in control of your thoughts and emotions"),("you are worthy of forgiveness and compassion"),("you are capable of overcoming challenges and obstacles"),("you are deserving of self-care and self-love"),("you have the strength to let go of negativity and embrace positivity"),("you are surrounded_by beauty_and inspiration"),("you are grateful for the present moment_and all that it brings"),("you are confident in your own skin_and embrace your individuality"),("you are at peace with your past_and excited for your future"),("you have nothing_to live for until you have something_to die for"),("the blessings you give to others will be given to more people than just the person you blessed" "your presence is a gift to the world", "you are a beacon of light and hope for those around you" "peoples words only have as much power as you give them" "joy is everywhere, you just have to look" ,"every end means another begining" "you are loved" "the more you struggle the stronger you become", "the light can always be turned back on" , "your eyes always ajust when your in the dark"))
    affirmation = random.choice(affirmations)
    return affirmation

 <!DOCTYPE html> # type: ignore
<html lang="en"> # type: ignore
<head> # type: ignore
  <meta charset="utf-8" /> # type: ignore
  <meta name="viewport" content="width=device-width,initial-scale=1" /> # type: ignore
  <title>Art Gallery — Import Your Art</title> # type: ignore
  <link rel="stylesheet" href="styles.css" /> # type: ignore
</head> # type: ignore 
<body># type: ignore 
  <header># type: ignore 
    <h1>Art Gallery</h1># type: ignore 
    <p>Import your artwork — drag & drop or use the button.</p># type: ignore 
  </header># type: ignore 
 

  <main> # type: ignore 
    <section class="controls"># type: ignore 
      <label class="button" for="fileInput">Import Images</label># type: ignore 
      <input id="fileInput" type="file" accept="image/*" multiple hidden /># type: ignore 
      <div id="dropZone" class="drop-zone">Drop images here</div> # type: ignore 
      <button id="clearBtn" class="small">Clear All</button> # type: ignore 
    </section> # type: ignore 
  <main> # type: ignore 
    <section id="gallery" class="gallery"></section># type: ignore 

 <section id="gallery" class="gallery" aria-live="polite"></section> # type: ignore
  </main> # type: ignore


<div id="viewer" class="viewer hidden" role="dialog" aria-modal="true"> # type: ignore
<button id="viewerClose" class="viewer-close">✕</button> # type: ignore
<img id="viewerImg" alt="" /> # type: ignore
 <div id="viewerMeta" class="viewer-meta"></div>  # type: ignore
  </div> # type: ignore 

<script src="script.js"></script> # type: ignore 
</body> # type: ignore 
</html> # type: ignore 

function downloadImageAsPdf(img) {
  const element = document.createElement('div');
  element.style.padding = '10px';
  element.style.backgroundColor = '#fff';

  const imgEl = document.createElement('img');
  imgEl.src = img.data;
  imgEl.style.maxWidth = '100%';
  imgEl.style.height = 'auto';

  element.appendChild(imgEl);

    const opt = } 
    margin: 10,
 filename: (img.name || 'artwork').replace(/\.[^/.]+$/, '') + '.pdf',
    image: { type: 'jpeg', quality: 0.98 },
     html2canvas: { scale: 2 },
    jsPDF: { orientation: 'portrait', unit: 'mm', format: 'a4' }

      html2pdf().set(opt).from(element).save();

async function downloadAllAsPdf(images) {
  if(images.length === 0) 
    alert('No images to download')
    return;
 }

   const opt = {
    margin: 10,
    filename: 'art-gallery.pdf',
      image: { type: 'jpeg', quality: 0.98 },
    html2canvas: { scale: 2 },
    jsPDF: { orientation: 'portrait', unit: 'mm', format: 'a4' }
  };

    const container = document.createElement('div');
  container.style.backgroundColor = '#fff';

  for (let i = 0; i < images.length; i++) 
    const img = images[i];
    const pageDiv = document.createElement('div');
    pageDiv.style.pageBreakAfter = 'always';
    pageDiv.style.padding = '10px';
     pageDiv.style.textAlign = 'center';

  const titleEl = document.createElement('h2');
    titleEl.textContent = img.name || 'Untitled';
    titleEl.style.marginBottom = '10px';
    pageDiv.appendChild(titleEl);

       const imgEl = document.createElement('img');
    imgEl.src = img.data;
  imgEl.style.maxWidth = '90%';
    imgEl.style.maxHeight = '80vh';

      pageDiv.appendChild(imgEl);
    container.appendChild(pageDiv);
  {

    html2pdf().set(opt).from(container).save();
}

const LIKES_KEY = 'artGallery.likes.v1';
let likedImages = new Set();
const viewerLikeBtn = document.getElementById('viewerLikeBtn');

function saveLikes() {
  try {
    localStorage.setItem(LIKES_KEY, JSON.stringify(Array.from(likedImages)));
      } catch (e) {
    console.warn('Could not save liked image IDs', e);
  }
}

function loadLikes() {
  const raw = localStorage.getItem(LIKES_KEY);
  if (!raw) return;
  try {
      likedImages = new Set(JSON.parse(raw));
  } catch (e) {
    likedImages = new Set();
  }
}

function likeImage(id) {
  const image = images.find(i => i.id === id);
  if (!image || likedImages.has(id)) return;

   image.likes = (image.likes || 0) + 1;
  likedImages.add(id);
    save();
  saveLikes();
  render();
}
function updateViewerLikeButton() {
  if (!currentViewingImage) return;
    if (likedImages.has(currentViewingImage.id)){
    viewerLikeBtn.textContent = 'Liked';
    viewerLikeBtn.disabled = true;
  } else {
       viewerLikeBtn.textContent = '❤️ Like';
    viewerLikeBtn.disabled = false;
  }
}

viewerLikeBtn.addEventListener('click', () => {
  if (currentViewingImage) likeImage(currentViewingImage.id);
});

const COMMUNITY_POSTS_KEY = 'artGallery.community.posts.v1';
const BOOKMARKS_KEY = 'artGallery.bookmarks.v1';

let communityPosts = [];
let bookmarkedPostIds = new Set();
let currentViewingCommunityPost = null;

function saveCommunityPosts() {
  try {
    localStorage.setItem(COMMUNITY_POSTS_KEY, JSON.stringify(communityPosts));
  } catch (e) {
       console.warn('Could not save community posts', e);
  }
}

function loadCommunityPosts() {
  const raw = localStorage.getItem(COMMUNITY_POSTS_KEY);
  if (!raw) return;
  try {
       communityPosts = JSON.parse(raw);
  } catch (e) {
    communityPosts = [];
  }
}

function saveBookmarks() {
  try {
    localStorage.setItem(BOOKMARKS_KEY, JSON.stringify(Array.from(bookmarkedPostIds)));
  } catch (e) {
       console.warn('Could not save bookmarks', e);
  }
}

function loadBookmarks() {
  const raw = localStorage.getItem(BOOKMARKS_KEY);
  if (!raw) return;
   try {
    bookmarkedPostIds = new Set(JSON.parse(raw));
  } catch (e) {
      bookmarkedPostIds = new Set();
  }
}

function shareToiCommunity(imageId) {
  const image = images.find(i => i.id === imageId);
  if (!image) return;

    const post = {
    id: 'post-' + Date.now().toString(36) + Math.random().toString(36).slice(2),
    originalImageId: imageId,
     name: image.name,
    data: image.data,
    likes: 0,
    author: 'Anonymous',
    createdAt: Date.now(),
      sharedFrom: 'My Gallery'
  };

    communityPosts.unshift(post);
  saveCommunityPosts();
  renderCommunityGallery();

    alert(`✅ "${image.name}" shared to community!`);
}

function bookmarkPost(postId) {
  const post = communityPosts.find(p => p.id === postId);
  if (!post || bookmarkedPostIds.has(postId)) return;

   bookmarkedPostIds.add(postId);
  saveBookmarks();
    renderCommunityGallery();
      renderBookmarksGallery();
  updateViewerBookmarkButton();

  alert('🔖 Post bookmarked!');
}

function removeBookmark(postId) {
bookmarkedPostIds.delete(postId);
saveBookmarks();
renderCommunityGallery();
renderBookmarksGallery();
updateViewerBookmarkButton();
}

function renderCommunityGallery() {
const communityGallery = document.getElementById('communityGallery');

  if (communityPosts.length === 0) {
    communityGallery.innerHTML = '<p style="color:var(--muted);text-align:center">No posts in community yet — share your artwork!</p>';
    return;
  }

  communityGallery.innerHTML = '';
  communityPosts.forEach(post => {
  const card = document.createElement('div');
  card.className = 'card';

  const imageEl = document.createElement('img');
    imageEl.className = 'thumb';
     imageEl.src = post.data;
    imageEl.alt = post.name || 'Community Artwork';
    imageEl.addEventListener('click', () => openCommunityViewer(post));

    const meta = document.createElement('div');
    meta.className = 'meta';

 const name = document.createElement('div');
 name.textContent = post.name || 'Untitled';

 const actions = document.createElement('div');
 actions.className = 'actions';

 const likeCount = document.createElement('span');
    likeCount.className = 'likes';
    likeCount.textContent = `${post.likes || 0} ❤️`;

        const bookmarkBtn = document.createElement('button');
    bookmarkBtn.className = 'action-btn';
     bookmarkBtn.title = bookmarkedPostIds.has(post.id) ? 'Remove bookmark' : 'Bookmark this post';
    bookmarkBtn.textContent = bookmarkedPostIds.has(post.id) ? '🔖 Bookmarked' : '🔖 Bookmark';
     bookmarkBtn.disabled = bookmarkedPostIds.has(post.id);
    bookmarkBtn.addEventListener('click', (e) => {
         e.stopPropagation();
      if (bookmarkedPostIds.has(post.id)) {
        removeBookmark(post.id);
           } else {
        bookmarkPost(post.id);
      }
    });

        actions.appendChild(likeCount);
    actions.appendChild(bookmarkBtn);

     meta.appendChild(name);
    meta.appendChild(actions);

     card.appendChild(imageEl);
    card.appendChild(meta);
    communityGallery.appendChild(card);
  });
}

function renderBookmarksGallery() {
  const bookmarksGallery = document.getElementById('bookmarksGallery');

   const bookmarkedPosts = communityPosts.filter(p => bookmarkedPostIds.has(p.id));

    if (bookmarkedPosts.length === 0) {
    bookmarksGallery.innerHTML = '<p style="color:var(--muted);text-align:center">No bookmarks yet — explore the community!</p>';
    return;
  }

   bookmarksGallery.innerHTML = '';
  bookmarkedPosts.forEach(post => {
  const card = document.createElement('div');
  card.className = 'card';

const imageEl = document.createElement('img');
    imageEl.className = 'thumb';
      imageEl.src = post.data;
    imageEl.alt = post.name || 'Bookmarked Artwork';
    imageEl.addEventListener('click', () => openCommunityViewer(post));

const meta = document.createElement('div');
    meta.className = 'meta';

     const name = document.createElement('div');
    name.textContent = post.name || 'Untitled';

      const actions = document.createElement('div');
    actions.className = 'actions';

       const likeCount = document.createElement('span');
    likeCount.className = 'likes';
    likeCount.textContent = `${post.likes || 0} ❤️`;

      const unbookmarkBtn = document.createElement('button');
    unbookmarkBtn.className = 'action-btn remove';
    unbookmarkBtn.textContent = '🗑️ Remove';
    unbookmarkBtn.addEventListener('click', (e) => {
      e.stopPropagation();
      removeBookmark(post.id);
    });

     actions.appendChild(likeCount);
    actions.appendChild(unbookmarkBtn);

    meta.appendChild(name);
    meta.appendChild(actions);

   card.appendChild(imageEl);
    card.appendChild(meta);
    bookmarksGallery.appendChild(card);
  });
}

function openCommunityViewer(post) {
  currentViewingCommunityPost = post;
  currentViewingImage = null;

 viewerImg.src = post.data;
  viewerImg.alt = post.name || 'Artwork';
  viewerMeta.textContent = `${post.name || 'Untitled'} · ${post.likes || 0} like${post.likes === 1 ? '' : 's'} · Shared: ${new Date(post.createdAt).toLocaleDateString()}`;
  
  viewer.classList.remove('hidden');
  updateViewerShareButton();
  updateViewerBookmarkButton();
}

function updateViewerShareButton() {
  const viewerShareBtn = document.getElementById('viewerShareBtn');
  const viewerBookmarkBtn = document.getElementById('viewerBookmarkBtn');

    if (currentViewingImage) {
    viewerShareBtn.style.display = 'inline-block';
   viewerBookmarkBtn.style.display = 'none';
  } else if (currentViewingCommunityPost) {
      viewerShareBtn.style.display = 'none';
    viewerBookmarkBtn.style.display = 'inline-block';
  }
}

function updateViewerBookmarkButton() {
  const viewerBookmarkBtn = document.getElementById('viewerBookmarkBtn');

    if (!currentViewingCommunityPost) return;

      if (bookmarkedPostIds.has(currentViewingCommunityPost.id)) {
    viewerBookmarkBtn.textContent = '🔖 Bookmarked';
    viewerBookmarkBtn.disabled = true;
      } else {
    viewerBookmarkBtn.textContent = '🔖 Bookmark';
    viewerBookmarkBtn.disabled = false;
  }
}

function switchTab(tabName) {
  document.querySelectorAll('.tab-content').forEach(el => {
    el.classList.add('hidden');
  });

  document.querySelectorAll('.tab-btn').forEach(btn => {
    btn.classList.remove('active');
  });

  if (tabName === 'gallery') {
    document.getElementById('gallery').classList.remove('hidden');
    document.getElementById('galleryControls').style.display = 'flex';
  } else if (tabName === 'community') {
    document.getElementById('communityTab').classList.remove('hidden');
    document.getElementById('galleryControls').style.display = 'none';
    renderCommunityGallery();
      } else if (tabName === 'bookmarks') {
    document.getElementById('bookmarksTab').classList.remove('hidden');
    document.getElementById('galleryControls').style.display = 'none';
    renderBookmarksGallery();
  }

event.target.classList.add('active');
}

document.addEventListener('DOMContentLoaded', () => {
 const viewerShareBtn = document.getElementById('viewerShareBtn');
 const viewerBookmarkBtn = document.getElementById('viewerBookmarkBtn');

   viewerShareBtn?.addEventListener('click', () => {
    if (currentViewingImage) {
      shareToiCommunity(currentViewingImage.id);
    }
  });
    viewerBookmarkBtn?.addEventListener('click', () => {
    if (currentViewingCommunityPost) {
       if (!bookmarkedPostIds.has(currentViewingCommunityPost.id)) {
        bookmarkPost(currentViewingCommunityPost.id);
      }
    }
  });
    document.querySelectorAll('.tab-btn').forEach(btn => {
    btn.addEventListener('click', (e) => {
      switchTab(e.target.dataset.tab);
    });
  });
});
loadCommunityPosts();
loadBookmarks();

const fileInput = document.getElementById('fileInput');
const dropZone = document.getElementById('dropZone');
const galleryEl = document.getElementById('gallery');
const clearBtn = document.getElementById('clearBtn');
const downloadAllBtn = document.getElementById('downloadAllBtn');

const viewer = document.getElementById('viewer');
const viewerImg = document.getElementById('viewerImg');
const viewerVideo = document.getElementById('viewerVideo');
const viewerClose = document.getElementById('viewerClose');
const viewerMeta = document.getElementById('viewerMeta');
const downloadPdfBtn = document.getElementById('downloadPdfBtn');

const STORAGE_KEY = 'artGallery.images.v1';

let images = [];
let currentViewingImage = null;

function save() {
  try {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(images));
  } catch (e) {
    console.warn('Could not save media', e);
  }
}

function load() {
  const raw = localStorage.getItem(STORAGE_KEY);
  if (!raw) return;
    try {
    images = JSON.parse(raw).map(img => ({
      ...img,
       likes: img.likes || 0,
      type: img.type || (img.data && img.data.startsWith('data:video') ? 'video' : 'image')
        }));
  } catch (e) {
    images = [];
  }
}

function render() {
  galleryEl.innerHTML = '';
  if (images.length === 0) {
    galleryEl.innerHTML = '<p style="color:var(--muted);text-align:center">No media yet — import some art!</p>';
    return;
  }

    images.slice().reverse().forEach(item => {
    const card = document.createElement('div');
    card.className = 'card';

        const mediaEl = item.type === 'video' ? document.createElement('video') : document.createElement('img');
    mediaEl.className = 'thumb';
    mediaEl.src = item.data;

      if (item.type === 'video') {
      mediaEl.muted = true;
       mediaEl.loop = true;
      mediaEl.preload = 'metadata';
         } else {
      mediaEl.alt = item.name || 'Artwork';
    }

     mediaEl.addEventListener('click', () => openViewer(item));

    const meta = document.createElement('div');
    meta.className = 'meta';'

    const name = document.createElement('div');
    name.textContent = item.name || 'Untitled';

    const actions = document.createElement('div');
    actions.className = 'actions';

  const likeCount = document.createElement('span');
    likeCount.className = 'likes';
     likeCount.textContent = `${item.likes || 0} ❤️`;

    const likeBtn = document.createElement('button');
     likeBtn.className = 'like';
       likeBtn.textContent = likedImages.has(item.id) ? 'Liked' : 'Like';
    likeBtn.disabled = likedImages.has(item.id);
      likeBtn.addEventListener('click', () => likeImage(item.id));

      const removeBtn = document.createElement('button');
    removeBtn.className = 'remove';
 removeBtn.textContent = 'Remove';
    removeBtn.addEventListener('click', () => removeImage(item.id));

        actions.appendChild(likeCount);
    actions.appendChild(likeBtn);
    actions.appendChild(removeBtn);

    meta.appendChild(name);
    meta.appendChild(actions);

  card.appendChild(mediaEl);
    card.appendChild(meta);
    galleryEl.appendChild(card);
  });

 updateViewerLikeButton();
}

function addFiles(fileList) {
  const files = Array.from(fileList).filter(f => f.type && (f.type.startsWith('image') || f.type.startsWith('video')));
  if (files.length === 0) return;

    const readers = files.map(f => new Promise(resolve => {
    const reader = new FileReader();
    reader.onload = e => resolve({
   name: f.name,
  data: e.target.result,
   id: Date.now().toString(36) + Math.random().toString(36).slice(2),
      created: Date.now(),
      likes: 0,  
 type: f.type.startsWith('video') ? 'video' : 'image'
    });
    reader.readAsDataURL(f);
  }));

  Promise.all(readers).then(results => {
    images = images.concat(results);
    save();
      render();
  }).catch(err => console.error(err));
}

function displayViewerItem(item, source = 'gallery') {
    currentViewingImage = source === 'gallery' ? item : null;
    currentViewingCommunityPost = source === 'community' ? item : null;

  viewerImg.classList.add('hidden');
  viewerVideo.classList.add('hidden');
  viewerVideo.pause();
    viewerVideo.removeAttribute('src');
  viewerVideo.load();

  
const viewerVideo: HTMLElement | null
 viewerVideo.classList.remove('hidden');
    viewerVideo.play().catch(() => {});
  } else {
      viewerImg.src = item.data;
    viewerImg.alt = item.name || 'Artwork';
    viewerImg.classList.remove('hidden');
  }

    viewerMeta.textContent = `${item.name || 'Untitled'} · ${item.likes || 0} like${item.likes === 1 ? '' : 's'} · ${item.type === 'video' ? 'Video' : 'Photo'}`;
  viewer.classList.remove('hidden');
  updateViewerLikeButton();
  updateViewerShareButton();


function openViewer(img) {
  displayViewerItem(img, 'gallery');
}

function closeViewer() {
  viewer.classList.add('hidden');
  viewerImg.src = '';
    viewerVideo.pause();
  viewerVideo.removeAttribute('src');
    viewerVideo.load();
  currentViewingImage = null;
   currentViewingCommunityPost = null;
}

fileInput.addEventListener('change', e => addFiles(e.target.files));

['dragenter', 'dragover'].forEach(ev => dropZone.addEventListener(ev, e => { e.preventDefault(); dropZone.classList.add('drag'); }));
['dragleave', 'drop'].forEach(ev => dropZone.addEventListener(ev, e => { e.preventDefault(); dropZone.classList.remove('drag'); }));

dropZone.addEventListener('drop', e => {
  if (e.dataTransfer && e.dataTransfer.files) addFiles(e.dataTransfer.files);
});

clearBtn.addEventListener('click', () => {
  if (!confirm('Clear all saved media?')) return;
  images = [];
likedImages.clear();
  save();
  saveLikes();
  render();
});

downloadPdfBtn.addEventListener('click', () => {
  const activeItem = currentViewingImage || currentViewingCommunityPost;
   if (activeItem?.type === 'image') {
    downloadImageAsPdf(activeItem);
     } else {
    alert('PDF export is available for photos only.');
  }
});

downloadAllBtn.addEventListener('click', () => downloadAllAsPdf(images.filter(item => item.type === 'image')));

viewerClose.addEventListener('click', closeViewer);
viewer.addEventListener('click', e => { if (e.target === viewer) closeViewer(); });

load();
loadLikes();
render();



